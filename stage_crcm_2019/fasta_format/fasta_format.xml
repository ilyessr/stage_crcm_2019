<?xml version="1.0"?>
<tool id="fasta_format" name="Fasta Format" version="1.0">
    <description> Format a fasta file </description>
    <command><![CDATA[
    python3 $__tool_directory__/fasta_format.py -i $infile -o $output1 -w $width -f $format -inf $output2
    ]]></command>
    <inputs>
        <param name="infile" type="data" format="fasta" label="Fasta file" help="Fasta file to format"></param>
        <param name="width"   type="integer" value="60" label = "Width of sequences" help="Width of the fasta sequences"></param>
        <param name="format"  type="select"  display="checkboxes" multiple="true" label="Remove from the header">
            <option value="OS" >OrganismSpecies (OS)</option>
            <option value="GN" >GeneName (GN)</option>
            <option value="PE" >ProteinExistence (PE)</option>
            <option value="SV" >SequenceVersion (SV)</option>
        </param>
    </inputs>

    <outputs>
        <data format="fasta" name="output1" label="${tool.name}: formatted.fasta" />
        <data format="txt" name="output2" label="${tool.name}: info.txt" />
    </outputs>

    <help>

    This program consists of Parser a fasta file ,Format the headers, Write the new fasta file output
    </help>

</tool>

